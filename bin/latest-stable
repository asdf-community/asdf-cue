#!/usr/bin/env bash

set -euo pipefail

# shellcheck source=lib/utils.bash
source "$(dirname "$0")/../lib/utils.bash"

query="${1:-}"
query="${query#latest:}"
[ "$query" = "latest" ] && query=""

latest_stable_version "$query"
